# 当前进度记录（2026-06-27）

本文档用于下次接手时快速恢复上下文。当前项目路径：`D:\cv`。

## 1. 当前结论

当前最强验证集方案是：

```text
YOLO11m RGBT + TTA
+ GEM adapter last
+ WBF 融合
```

验证集 COCO 评测结果：

| 方案 | AP50:95 | AP50 |
| --- | ---: | ---: |
| 小模型最高分：GEM adapter last + YOLO11s RGBT-2 TTA + WBF | 0.618 | 0.812 |
| YOLO11m 单模型 | 0.610 | 0.804 |
| YOLO11m + TTA | 0.623 | 0.817 |
| YOLO11m-TTA + GEM adapter WBF | 0.630 | 0.821 |

当前推荐提交文件：

```text
submissions/final_reproduce_big_yolo11m_tta_adapter_wbf075_bbox3.json
```

该文件已通过 `make_submission.py` 校验：

```text
检测框数量：81808
image_id：1-1000
category_id：[1,2,3,4,5]
bbox 宽高均大于 0
```

注意：提交文件必须保留 `bbox-decimals=3`。如果只保留 1 位小数，极小 bbox 可能被四舍五入成 `0.0`，导致评测端“提交文件识别失败”。

## 2. 关键权重与模型大小

### YOLO11m 大模型

权重：

```text
work_dirs/yolo11m_rgbt_aug_domain_w0/weights/best.pt
```

训练状态：

```text
训练已完成，exit code 0
best epoch：96
early stopping：196 epoch
训练耗时：约 25.96 小时
```

参数量：

```text
约 20.01M 参数
```

权重文件大小：

```text
约 40.5 MB
```

### YOLO11s 小模型

主权重：

```text
work_dirs/yolo11s_rgbt_aug_domain-2/weights/best.pt
```

按 `sum(p.numel() for p in model.parameters())` 测得：

```text
9429727
Params: 9.43M
```

### GEM adapter 小模型融合成员

权重：

```text
work_dirs/gem_adapter_yolo_pretrained_fix/weights/last.pt
```

按同样参数统计方式测得：

```text
GEM adapter + YOLO11s: 9.45M
Adapter only: 0.0173M
YOLO inside adapter: 9.43M
```

GEM adapter 只是给 YOLO11s 前面加了很小的适配器，额外参数量很低。

## 3. 已完成训练与推理

### YOLO11m 训练

启动脚本：

```text
scripts/start_yolo11m_rgbt_aug_domain_train.cmd
```

训练输出目录：

```text
work_dirs/yolo11m_rgbt_aug_domain_w0
```

训练日志：

```text
work_dirs/yolo11m_rgbt_aug_domain_w0_train.out.log
work_dirs/yolo11m_rgbt_aug_domain_w0_launcher.log
```

训练时为了 Windows 后台稳定，使用：

```text
workers=0
MPLBACKEND=Agg
PYTHONUNBUFFERED=1
```

普通后台启动会被当前受限进程树清理，需要用提升权限或外部 PowerShell 启动。

### 小模型最高分复现

最终提交文件：

```text
submissions/final_reproduce_small_wbf075_bbox3.json
```

流程：

```text
GEM adapter last
+ YOLO11s RGBT-2 TTA
+ WBF(iou=0.75, weights=1.0/0.7, primary_count=1)
```

中间文件：

```text
submissions/reproduce_small_gem_adapter_last_test.json
submissions/reproduce_small_rgbt2_tta_test.json
submissions/phase0_search/reproduce_small_wbf075_test.json
```

输出检测框数量：

```text
50397
```

### 大模型最高分复现

最终提交文件：

```text
submissions/final_reproduce_big_yolo11m_tta_adapter_wbf075_bbox3.json
```

流程：

```text
YOLO11m RGBT best.pt + TTA
+ GEM adapter last
+ WBF(iou=0.75, weights=1.0/0.7, primary_count=1)
```

中间文件：

```text
submissions/reproduce_yolo11m_tta_test.json
submissions/reproduce_big_gem_adapter_last_test.json
submissions/phase0_search/reproduce_big_yolo11m_tta_adapter_wbf075_test.json
```

输出检测框数量：

```text
81808
```

## 4. 复现命令

### 大模型 TTA 推理

```powershell
& 'D:\Anaconda\envs\yolo_gaiic\python.exe' scripts\predict_rgbt_to_coco.py `
  --model work_dirs\yolo11m_rgbt_aug_domain_w0\weights\best.pt `
  --source datasets\gaiic_yolo_rgbt_aug_domain\images\test `
  --out submissions\reproduce_yolo11m_tta_test.json `
  --data-root "GAIIC2024-赛道1-目标检测任务" `
  --split test `
  --imgsz 640 `
  --conf 0.001 `
  --iou 0.7 `
  --max-det 300 `
  --device 0 `
  --batch-size 16 `
  --augment
```

### GEM adapter 推理

```powershell
& 'D:\Anaconda\envs\yolo_gaiic\python.exe' scripts\predict_gem_adapter_yolo_to_coco.py `
  --checkpoint work_dirs\gem_adapter_yolo_pretrained_fix\weights\last.pt `
  --out submissions\reproduce_big_gem_adapter_last_test.json `
  --data-root "GAIIC2024-赛道1-目标检测任务" `
  --split test `
  --imgsz 640 `
  --conf 0.001 `
  --iou 0.7 `
  --max-det 300 `
  --device 0 `
  --batch-size 4
```

### WBF 融合

```powershell
& 'D:\Anaconda\envs\yolo_gaiic\python.exe' scripts\fuse_multi_coco_results.py `
  --inputs submissions\reproduce_yolo11m_tta_test.json submissions\reproduce_big_gem_adapter_last_test.json `
  --weights 1.0 0.7 `
  --out submissions\phase0_search\reproduce_big_yolo11m_tta_adapter_wbf075_test.json `
  --method wbf `
  --iou-thr 0.75 `
  --score-thr 0.001 `
  --max-per-image 300 `
  --primary-count 1
```

### 生成规范提交

```powershell
& 'D:\Anaconda\envs\yolo_gaiic\python.exe' scripts\make_submission.py `
  --input submissions\phase0_search\reproduce_big_yolo11m_tta_adapter_wbf075_test.json `
  --out submissions\final_reproduce_big_yolo11m_tta_adapter_wbf075_bbox3.json `
  --bbox-decimals 3 `
  --score-decimals 6
```

## 5. 进一步建议

1. 不建议重新训练同配置 YOLO11m。当前训练已经到 best epoch 96，并在 196 epoch 早停。
2. 如果继续冲分，优先训练 `YOLO11m seed1/seed2`，作为新的融合成员。
3. 不建议优先训练 RGB-only 或 TIR-only 大模型。之前 RGB/TIR 加入融合收益不稳定，甚至拉低结果。
4. 可以继续做 WBF 参数搜索，重点围绕：

```text
YOLO11m-TTA + GEM adapter
```

建议搜索范围：

```text
iou-thr: 0.65 / 0.70 / 0.75 / 0.80 / 0.85
weights: 1.0/0.5, 1.0/0.6, 1.0/0.7, 1.0/0.8
score-thr: 0.001
max-per-image: 300
primary-count: 1
```

## 6. 重要注意事项

- `submissions/phase0_search/yolo11m_tta_adapter_wbf075_test.json` 曾因提交格式化时 bbox 只保留 1 位小数，导致极小高度如 `0.037` 变为 `0.0`，评测端识别失败。
- 解决方式：最终提交一律使用 `--bbox-decimals 3`。
- 当前最推荐提交文件是：

```text
submissions/final_reproduce_big_yolo11m_tta_adapter_wbf075_bbox3.json
```

