# RGB-T 两次训练对比记录

## 1. 实验版本

| 项目 | 基线训练 | 测试域增强训练 |
| --- | --- | --- |
| 模型 | YOLO11s | YOLO11s |
| 初始化 | `yolo11s.pt` COCO 预训练 | `yolo11s.pt` COCO 预训练 |
| 数据配置 | `configs/gaiic_yolo_rgbt.yaml` | `configs/gaiic_yolo_rgbt_aug_domain.yaml` |
| 训练数据 | `datasets/gaiic_yolo_rgbt` | `datasets/gaiic_yolo_rgbt_aug_domain` |
| 输出目录 | `D:\cv\work_dirs\yolo11s_rgbt` | `D:\cv\work_dirs\yolo11s_rgbt_aug_domain` |
| epochs | 100 | 100 |
| batch | 8 | 8 |
| imgsz | 640 | 640 |

两次训练均为 RGB-T 输入级融合方案，融合图像为三通道输入，YOLO 网络结构未修改。

## 2. 最佳验证指标

按 `metrics/mAP50-95(B)` 选择最佳 epoch。

| 指标 | 基线训练 | 测试域增强训练 | 变化 |
| --- | ---: | ---: | ---: |
| best epoch | 87 | 88 | +1 |
| Precision | 84.613% | 84.177% | -0.436 个百分点 |
| Recall | 79.241% | 81.099% | +1.858 个百分点 |
| mAP50 | 85.383% | 86.267% | +0.884 个百分点 |
| mAP50-95 | 64.354% | 65.069% | +0.715 个百分点 |

## 3. 最后一轮指标

| 指标 | 基线训练 epoch 100 | 测试域增强训练 epoch 100 | 变化 |
| --- | ---: | ---: | ---: |
| Precision | 84.734% | 82.527% | -2.207 个百分点 |
| Recall | 79.487% | 83.034% | +3.547 个百分点 |
| mAP50 | 85.234% | 86.074% | +0.840 个百分点 |
| mAP50-95 | 64.287% | 64.892% | +0.605 个百分点 |

## 4. 结论

测试域增强训练提升了召回率和整体 AP：

- 最佳 `mAP50-95` 从 `64.354%` 提升到 `65.069%`。
- 最后一轮 `mAP50-95` 从 `64.287%` 提升到 `64.892%`。
- Recall 提升明显，说明漏检减少。
- Precision 下降，说明误检略有增加。

整体看，测试域增强方案对当前验证集是正收益，主要收益来自更高召回率。

## 5. 权重与结果文件

基线权重：

```text
D:\cv\work_dirs\yolo11s_rgbt\weights\best.pt
D:\cv\work_dirs\yolo11s_rgbt\weights\last.pt
```

增强版权重：

```text
D:\cv\work_dirs\yolo11s_rgbt_aug_domain\weights\best.pt
D:\cv\work_dirs\yolo11s_rgbt_aug_domain\weights\last.pt
```

基线训练结果：

```text
D:\cv\work_dirs\yolo11s_rgbt\results.csv
D:\cv\work_dirs\yolo11s_rgbt\results.png
D:\cv\work_dirs\yolo11s_rgbt\confusion_matrix.png
```

增强版训练结果：

```text
D:\cv\work_dirs\yolo11s_rgbt_aug_domain\results.csv
D:\cv\work_dirs\yolo11s_rgbt_aug_domain\results.png
D:\cv\work_dirs\yolo11s_rgbt_aug_domain\confusion_matrix.png
```

增强版测试集提交：

```text
D:\cv\submissions\final_submission_aug_domain.json
```

增强版测试集提交检测框数量：

```text
77490
```

## 6. 域增强分析文件

```text
D:\cv\analysis\domain_stats.json
D:\cv\analysis\domain_shift_report.md
D:\cv\configs\domain_aug_rgbt.json
```

## 7. Loss 曲线文件

```text
D:\cv\analysis\loss_curves\baseline_loss.png
D:\cv\analysis\loss_curves\aug_domain_loss.png
D:\cv\analysis\loss_curves\loss_comparison.png
D:\cv\analysis\loss_curves\loss_summary.csv
```
