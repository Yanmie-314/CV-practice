# RGB-T 双光目标检测改进方案与实现指南（更新版）

> **文档用途**：供下一个 Agent 阅读，明确改进目标、论文来源、实现路径，并直接动手实现。
> **当前项目位置**：`/Users/xinfloria/Desktop/my-project/CV-practice`
> **当前最佳性能**：mAP50 = 86.3%，mAP50-95 = 65.1%（val set，YOLO11s + 输入级 RGB-T 拼接）

## 📋 改进策略更新说明（2025-06-21）

基于 **MambaOut (CVPR 2025)** 论文的重要洞察，本方案已重新调整优先级：

### MambaOut 核心结论
```
Mamba 适合的任务需满足两个特征：
├─ 长序列 (Long-sequence)：token 数量大，Transformer 二次复杂度成瓶颈
└─ 自回归 (Autoregressive)：需要因果 token 混合

视觉任务分析：
├─ 图像分类：❌ 不满足任何一条 → Mamba 不必要
├─ 目标检测：✅ 满足"长序列" ❌ 不满足"自回归" → Mamba 可能有价值但非必需
└─ 语义分割：✅ 满足"长序列" ❌ 不满足"自回归" → Mamba 可能有价值但非必需
```

### 本方案调整

| 调整项 | 原方案 | 更新后 |
|--------|--------|--------|
| **最优先级训练改进** | MambaRefine-YOLO | **GEM-YOLO**（纯 CNN，无 Mamba） |
| **Mamba 方案定位** | 核心方案 | **对比实验**（验证必要性） |
| **WaveMamba** | 进阶方案 | **待定**（视 Mamba 实验结果） |
| **风险控制** | 高（依赖 mamba-ssm） | **低**（纯 PyTorch） |

### 执行路线图

```
Phase 0 (立即执行): 推理优化
├─ TTA + WBF 多模型集成
└─ 预期提升：+1~2%（不改模型）

Phase 1 (最优先): GEM-YOLO 实现 ⭐⭐⭐
├─ 轻量门控融合 (GFM)
├─ 小目标保留下采样 (SPD-Conv)
├─ 轻量化 Neck (Ghost + GSConv)
└─ 预期提升：+2~3%，风险极低

Phase 2-A (如果 Phase 1 效果好): YOLO11m 升级
└─ 更强大的 backbone

Phase 2-B (对比实验): Mamba 必要性验证
├─ 在 GEM-YOLO 基础上替换 Neck 为 HFAN
├─ 对比 Ghost Neck vs HFAN (Mamba-based)
└─ 决策：如果 HFAN 提升 > 1.5% → 继续深入 Mamba

Phase 3 (待定): WaveMamba
└─ 频域分解 + Mamba 融合（仅在 Phase 2-B 证明有效后）
```

> **核心原则**：先验证最稳妥的方案（GEM-YOLO），再决定是否引入复杂架构（Mamba）。

---

---

## 一、问题诊断（为什么现在效果不好）

### 1.1 当前架构的核心缺陷

```
当前方案：RGB(3ch) + TIR(1ch) → [TIR, G, R] 3通道拼接 → YOLO11s 单模型
```

| 缺陷 | 具体表现 |
|------|---------|
| **静态融合** | 白天/夜间使用相同的融合权重，夜间 RGB 退化的噪声直接污染模型 |
| **模态互补信息未充分挖掘** | 可见光富含高频纹理，红外富含低频热辐射，简单拼接无法利用这种互补性 |
| **小目标信息丢失** | 标准 stride=2 下采样在深层网络中将小目标特征完全抹除 |
| **域泛化差** | 测试集与训练集光照/天气/场景分布不同，静态融合无法自适应 |

### 1.2 已尝试但效果有限的方案

- 域增强训练（analyze_domain_shift + prepare_rgbt_domain_aug_dataset）：mAP50-95 仅提升 +0.7%
- 原因：**数据层面的改进已近极限，需要从网络结构上根本解决融合质量问题**

---

## 二、推荐改进方案

### ⚡ 方案一（最优先，预期最大收益）

#### 方案名称：双流自适应门控融合（基于 GEM-YOLO）

**类型**：训练优化（改变网络结构 + 重新训练）

**核心思想**：
- 放弃输入级通道拼接，改为**独立双流提取 + 特征级门控融合**
- 用一个轻量 GFM（Gated Fusion Module）根据全局场景上下文动态分配 RGB 和红外的贡献权重

**参考论文**：
```
GEM-YOLO: A Lightweight and Real-Time RGBT Object Detector with Gated Multimodal Fusion
发表：Sensors 2026, Vol.26(7), 2035
链接：https://www.mdpi.com/1424-8220/26/7/2035
```

**三个核心模块**：

#### 模块 A：GFM（门控融合模块）

```python
# 核心伪代码逻辑
class GFM(nn.Module):
    """
    Gated Fusion Module - 自适应门控融合
    输入：F_rgb, F_ir，形状均为 [B, C, H, W]
    输出：F_fused，形状 [B, C, H, W]
    """
    def forward(self, F_rgb, F_ir):
        # 1. 拼接后做全局平均池化，获取全局上下文向量
        v = GAP(Concat(F_rgb, F_ir))  # [B, 2C, 1, 1]
        
        # 2. 轻量 MLP 预测模态置信度权重
        weights = Softmax(W2(ReLU(W1(v))))  # [B, 2, 1, 1]
        α_rgb, α_ir = weights[:, 0], weights[:, 1]
        
        # 3. 加权融合
        F_fused = α_rgb * F_rgb + α_ir * F_ir
        return F_fused
```

**关键特性**：
- 白天场景：自动学习 α_rgb > α_ir
- 夜晚/弱光场景：自动学习 α_ir > α_rgb
- 参数极少（2个 1×1 Conv），几乎不增加计算量

#### 模块 B：SPD-Conv（保留小目标特征的下采样）

```python
# 替换传统 stride=2 卷积
class SPDConv(nn.Module):
    """
    Space-to-Depth + 深度可分离卷积
    解决小目标在反复下采样中消失的问题
    """
    def forward(self, x):
        # 空间 -> 通道重排（无信息损失的下采样）
        x = space_to_depth(x, block_size=2)  # [B, C, H, W] -> [B, 4C, H/2, W/2]
        # 深度可分离卷积（降低计算量约 87%）
        x = depthwise_conv(x)
        x = pointwise_conv(x)
        return x
```

#### 模块 C：Ghost Neck（轻量化 Neck）

用 Ghost Module 替换 C3k2/Bottleneck，用 GSConv 替换下采样，补偿双流带来的计算量增加，使总 GFLOPs 不超过单流基线。

**实现步骤（Agent 请依次执行）**：

```
Step 1: 阅读 https://www.mdpi.com/1424-8220/26/7/2035 了解完整架构

Step 2: 创建 scripts/models/gem_yolo.py，实现：
  - GFM 门控融合模块
  - SPDConv 小目标保留下采样
  - GhostBottleneck 轻量化 Neck 模块
  - GEM_YOLO_Detector 整体检测器类（双流输入）

Step 3: 创建 scripts/datasets/rgbt_dual_dataset.py
  - 修改数据加载，分别返回 (rgb_img, tir_img, labels)
  - 不再做通道拼接，保持双流独立输入

Step 4: 创建 configs/gem_yolo_rgbt.yaml
  - 指向双流数据集

Step 5: 创建 scripts/train_gem_yolo.py，训练脚本

Step 6: 训练并对比 val set mAP50/mAP50-95
```

**预期性能提升**：mAP50-95 提升 **+2~3%**（DroneVehicle 同任务实测 +2.5%）

---

### ⚡ 方案二（对比实验，验证 Mamba 必要性）

#### 方案名称：MambaRefine-YOLO 双关门 Mamba 融合

**类型**：训练优化（改变网络结构 + 重新训练）

**⚠️ 重要提示**：
> 基于 **MambaOut (CVPR 2025)** 的洞察，Mamba 对检测任务可能有价值（符合"长序列"特征），但非必需。建议先完成 **GEM-YOLO（方案一）** 验证，再决定是否引入 Mamba。

**为什么考虑**：
- 论文在 **DroneVehicle** 数据集上测试，该数据集与本项目任务**完全一致**（无人机视角，car/truck/bus/van/freight-car 五类车辆检测）
- DroneVehicle 上 mAP50 从 79.4%（concat baseline）提升到 **83.2%（+3.8%）**
- 但需注意：该提升来自 **DGC-MFM + HFAN 联合作用**，非单一 Mamba 贡献

**参考论文**：
```
MambaRefine-YOLO: A Dual-Modality Small Object Detector for UAV Imagery
arXiv: 2511.19134
链接：https://arxiv.org/abs/2511.19134
发表：2025年11月
```

**两个核心模块**：

#### 模块 A：DGC-MFM（双关门互补 Mamba 融合模块）

```python
class DGC_MFM(nn.Module):
    """
    Dual-Gated Complementary Mamba Fusion Module
    同时使用光照门和差异门
    """
    def forward(self, F_rgb, F_ir):
        # ---- 光照门 (Illumination Gate) ----
        # 估计各模态亮度
        L_rgb = GAP(Conv(F_rgb))  # 标量
        L_ir  = GAP(Conv(F_ir))   # 标量
        # 光照权重：亮时偏 RGB，暗时偏 IR
        W_light = Sigmoid(gamma * (L_rgb - L_ir))
        
        # ---- 差异门 (Difference Gate) ----
        # 计算特征差异，找到互补通道
        F_diff = |F_rgb - F_ir|
        A_diff = Softmax(W2(ReLU(W1(GAP(F_diff)))))
        W_diff_rgb, W_diff_ir = split(A_diff)
        
        # ---- 双关门融合 ----
        F_fused = (W_light * W_diff_rgb) * F_rgb + \
                  ((1 - W_light) * W_diff_ir) * F_ir
        
        # ---- 双向 Mamba 全局上下文 ----
        F_fused = BiMamba(F_fused)  # 线性复杂度的全局建模
        
        # 残差连接 + Fusion-Shuffle
        F_rgb_out = F_rgb + Proj(F_fused)
        F_ir_out  = F_ir  + Proj(F_fused)
        return F_rgb_out + F_ir_out
```

#### 模块 B：HFAN（分层特征聚合 Neck）

```python
class ASFB(nn.Module):
    """
    Adaptive Scale Fusion Block - HFAN 的核心单元
    "先精修，后融合" 策略
    """
    def forward(self, F_deep, F_mid, F_shallow):
        # 1. Content Reconstruction Upsampler
        #    用内容感知动态核恢复深层特征的细节
        F_deep_up = CARAFE(F_deep)  # 内容感知上采样
        
        # 2. Geometric Alignment Downsampler
        #    可变形卷积纠正几何扭曲
        F_shallow_down = DeformConv(F_shallow)
        
        # 3. Adaptive Weighted Fuser
        #    自适应加权三级特征
        w1, w2, w3 = learnable_weights()
        F_out = (w1*F_deep_up + w2*F_mid + w3*F_shallow_down) / (w1+w2+w3+eps)
        return F_out
```

**实现步骤（Agent 请依次执行）**：

```
Step 1: 精读 https://arxiv.org/abs/2511.19134

Step 2: 安装 mamba-ssm 依赖
  pip install mamba-ssm causal-conv1d

Step 3: 创建 scripts/models/mamba_refine_yolo.py，实现：
  - IlluminationGate
  - DifferenceGate
  - DGC_MFM（双关门Mamba融合）
  - ASFB（自适应尺度融合块）
  - HFAN（分层特征聚合Neck）
  - MambaRefineYOLO（整体检测器）

Step 4: 双流数据集加载（复用方案一的 rgbt_dual_dataset.py）

Step 5: 配置训练脚本，epochs=300, optimizer=AdamW, input=640×640

Step 6: 先单独测试 HFAN（把标准 FPN 替换为 HFAN，其余不变），验证 +1.1% 提升

Step 7: 再加入 DGC-MFM，验证完整方案性能
```

**预期性能提升**：mAP50 提升 **+3~5%**，mAP50-95 提升 **+2~3%**

---

### ⚡ 方案三（进阶，频域创新方向 - 待定）

#### 方案名称：WaveMamba 小波频域互补融合

**类型**：训练优化（改变网络结构 + 重新训练）

**⚠️ 前置条件**：
> 建议在完成以下验证后再考虑此方案：
> 1. ✅ GEM-YOLO 已实现并获得 baseline
> 2. ✅ MambaRefine-YOLO 对比实验证明 Mamba 有必要性
> 3. ⏳ GPU 资源充足（需额外安装 mamba-ssm 和 PyWavelets）

**参考论文**：
```
WaveMamba: Wavelet-Driven Mamba Fusion for RGB-Infrared Object Detection
arXiv: 2507.18173
链接：https://arxiv.org/abs/2507.18173
发表：2025年7月
```

**核心洞察**（论文中有实验数据支撑）：
- 红外图像的**低频子带（LL）** 信息熵更高 → 目标轮廓/热辐射
- 可见光图像的**高频子带（LH, HL, HH）** 信息熵更高 → 纹理/边缘细节

```python
class WMFB(nn.Module):
    """
    WaveMamba Fusion Block
    在频域中分别处理低频和高频特征
    """
    def forward(self, F_rgb, F_ir):
        # 离散小波变换（Haar）
        F_rgb_L, F_rgb_H = DWT(F_rgb)  # 低频 + 高频
        F_ir_L,  F_ir_H  = DWT(F_ir)
        
        # 低频 Mamba 融合（LMFB）
        # - SFM：通道交换 + VSS Block（浅层融合）
        # - DFM：门控注意力机制（深层融合）
        F_L_fused = LMFB(F_rgb_L, F_ir_L)
        
        # 高频增强（HFE）：取绝对值更大的那个
        F_H_fused = abs_max_fusion(F_rgb_H, F_ir_H)
        
        # 逆小波变换重建
        F_out = IDWT(F_L_fused, F_H_fused)
        return F_out
```

**实现步骤**：

```
Step 1: 精读 https://arxiv.org/abs/2507.18173

Step 2: 安装 PyWavelets 或使用 torch 实现 Haar DWT/IDWT
  pip install PyWavelets

Step 3: 创建 scripts/models/wave_mamba.py，实现：
  - HaarDWT / HaarIDWT（PyTorch 可微分版本）
  - SFM（浅层 Mamba 融合 + 通道交换）
  - DFM（深层 Mamba 融合 + 门控注意力）
  - WMFB（WaveMamba Fusion Block）
  - WaveMamba_YOLO（基于 YOLOv8 backbone）

Step 4: 在 configs/wave_mamba_rgbt.yaml 配置数据集路径

Step 5: 训练并验证
```

**预期性能**：DroneVehicle mAP50 **79.8%**，比 single-modal 基线提升约 **+8%**

---

## 三、推理优化方案（不改模型，直接提分）

> 以下方案**不需要重新训练**，是在现有模型基础上提升推理性能

### 🔧 推理优化 A：TTA（测试时增强）

**原理**：对同一张图像做多次不同增强（flip、scale），将多个预测结果用 WBF 合并

```bash
# 在推理时开启 TTA
conda run -n yolo_gaiic yolo detect predict \
  model=work_dirs/yolo11s_rgbt_aug_domain/weights/best.pt \
  source=datasets/gaiic_yolo_rgbt_aug_domain/images/test \
  augment=True \  # 开启 TTA
  conf=0.001 iou=0.7 max_det=300 device=0
```

**预期提升**：+0.5~1.5%

### 🔧 推理优化 B：多模型 WBF 集成

**原理**：同时训练 RGB-only、TIR-only、RGB-T 三个模型，用 WBF 合并三个模型的预测框

当前项目已有 `scripts/fuse_coco_results.py`，扩展为三模型集成：

```bash
# Step1: 生成三个模型的预测
python scripts/predict_rgbt_to_coco.py --model work_dirs/yolo11s_rgb/weights/best.pt ...
python scripts/predict_rgbt_to_coco.py --model work_dirs/yolo11s_tir/weights/best.pt ...
python scripts/predict_rgbt_to_coco.py --model work_dirs/yolo11s_rgbt_aug_domain/weights/best.pt ...

# Step2: WBF 三模型融合（修改 fuse_multi_coco_results.py）
python scripts/fuse_multi_coco_results.py \
  --inputs submissions/rgb_test.json submissions/tir_test.json submissions/rgbt_test.json \
  --weights 0.3 0.3 0.4 \
  --iou-thr 0.55 --skip-box-thr 0.001 \
  --out submissions/wbf_ensemble_test.json
```

**预期提升**：+1~2%

### 🔧 推理优化 C：NMS 阈值精细调优

当前使用 iou=0.7，密集场景下可能过滤掉紧邻目标：

```bash
# 对 val set 搜索最优 conf + iou 阈值
for conf in 0.001 0.01 0.05; do
  for iou in 0.5 0.6 0.65 0.7 0.75; do
    # 推理 + 评估
    python scripts/eval_coco_results.py ...
  done
done
```

---

## 四、优先级与时间规划（更新版）

> **基于 MambaOut (CVPR 2025) 的洞察调整**：Mamba 对图像分类不必要，对检测/分割可能有价值但非必需。因此优先验证无 Mamba 的 GEM-YOLO，再决定是否引入 Mamba。

| 优先级　　　　　 | 方案　　　　　　　　　　　　| 类型　　 | 预计耗时　| 预期 mAP50-95 提升 | 风险等级 |
| ------------------| -----------------------------| ----------| -----------| --------------------| ----------|
| 🥇 **Phase 0**　 | 多模型 WBF 集成　　　　　　 | 推理优化 | 2小时　　 | +1~2%　　　　　　　| 低　　　 |
| 🥇 **Phase 0**　 | TTA 开启　　　　　　　　　　| 推理优化 | 30分钟　　| +0.5~1%　　　　　　| 低　　　 |
| 🏆 **Phase 1**　 | **GEM-YOLO 门控融合**　　　 | 训练优化 | 1~2天　　 | +2~3%　　　　　　　| **极低** |
| 🥈 **Phase 2-A** | 升级 YOLO11m　　　　　　　　| 训练优化 | 4小时训练 | +2~3%　　　　　　　| 低　　　 |
| 🥉 **Phase 2-B** | MambaRefine-YOLO (对比实验) | 训练优化 | 3~5天　　 | +3~5%　　　　　　　| **高**　 |
| 📋 **Phase 3**　 | WaveMamba (待定)　　　　　　| 训练优化 | 1周　　　 | +5~8%　　　　　　　| **高**　 |

### 优先级说明

```
执行顺序：
Phase 0 → Phase 1 → [根据 Phase 1 结果决定 Phase 2-A 还是 Phase 2-B]

决策节点：
├─ 如果 GEM-YOLO 达到 mAP50-95 > 67% → 优先做 Phase 2-A (YOLO11m 升级)
├─ 如果 GEM-YOLO 提升有限 (< +1.5%) → 尝试 Phase 2-B (Mamba)
└─ 如果 Mamba 对比实验效果不明显 → 放弃 Phase 3
```

---

## 五、给下一个 Agent 的明确任务清单（更新版）

### 任务 1（Phase 0：最快，立即执行）
```
目标：开启 TTA + WBF 多模型集成，不改网络结构，快速提分
步骤：
1. 确认 TIR-only 模型是否已训练（work_dirs/yolo11s_tir/），如未训练则先训练
2. 对 val set 分别跑 rgb/tir/rgbt 三个模型推理
3. 用 fuse_multi_coco_results.py 做 WBF 集成，调优权重
4. 评估并记录提升
预期：mAP50-95 +1~2%
```

---

### 任务 2（Phase 1：核心训练改进，最优先）

> **这是当前最推荐的训练改进方案** — 基于 GEM-YOLO (Sensors 2026)，无 Mamba 依赖，工程风险最低。

```
目标：实现 GEM-YOLO 门控融合模块，替换当前的输入级拼接
原因：
  - 轻量门控机制（GFM）替代静态拼接，自适应调整 RGB/IR 权重
  - SPD-Conv 保留小目标特征，解决下采样信息丢失
  - Ghost Neck 补偿双流计算开销，保持实时性
  - 无 Mamba/Transformer，纯 CNN 架构，训练稳定

需要阅读的论文：https://www.mdpi.com/1424-8220/26/7/2035
关键部分：
  - Section 3.2: GFM (Gated Fusion Module) - 全局门控融合
  - Section 3.3: SPD-Conv - 小目标保留下采样
  - Section 3.4: Ghost Neck - 轻量化特征聚合

实现步骤：
  Step 1: 创建 scripts/models/gem_yolo.py
          - 实现 GFM 模块 (GAP + MLP + Softmax 门控)
          - 实现 SPD-Conv 模块 (Space-to-Depth + Depthwise Sep Conv)
          - 实现 GhostBottleneck 模块
          - 组装 GEM_YOLO_Detector 类
  
  Step 2: 创建 scripts/datasets/rgbt_dual_dataset.py
          - 修改数据加载，分别返回 (rgb_img, tir_img, labels)
          - 不再做通道拼接，保持双流独立输入
  
  Step 3: 创建 configs/gem_yolo_rgbt.yaml
          - 配置双流输入、GFM 融合位置 (P3/P4/P5)
  
  Step 4: 创建 scripts/train_gem_yolo.py，启动训练
          - 训练参数：epochs=150, batch=16, lr=0.004 (SGD)
          - 禁用 AMP (多模态融合需要数值稳定性)
  
  Step 5: 评估并记录
          - 对比基准：当前 mAP50=86.3%, mAP50-95=65.1%
          - 目标提升：mAP50-95 +2~3%

参考性能（论文报告）：
  - FLIR 数据集：mAP@50 = 82.8%, mAP@50:95 = 46.5%
  - M3FD 数据集：mAP@50 = 69.0%, mAP@50:95 = 43.8%
  - 相比 YOLOv11n 双流基线：+1.0% mAP@50, -18.6% GFLOPs
```

---

### 任务 3（Phase 2-B：对比实验，视 Phase 1 结果而定）

> **Mamba 方案作为对比实验，非必选项** — 基于 MambaOut (CVPR 2025) 的洞察。

```
目标：对比验证 Mamba 在 RGB-T 无人机检测中的必要性
背景：
  - MambaOut 论文指出：Mamba 对图像分类不必要
  - 对检测/分割可能有价值（符合"长序列"特征），但非必需
  - 我们的任务（640×640 输入 ≈ 1600 tokens）符合长序列特征

实验设计：
  对照组：GEM-YOLO（无 Mamba）
  实验组：MambaRefine-YOLO（有 Mamba）
  
  只替换 Neck：
    - GEM-YOLO 保持 GFM 融合模块不变
    - 将 GEM-YOLO 的 Neck 替换为 HFAN（Hierarchical Feature Aggregation Neck）
    - 对比是否引入 Mamba 带来的提升

决策标准：
  - 如果 HFAN 比 Ghost Neck 提升 > 1.5% → 继续深入 Mamba
  - 如果提升 < 1% → 放弃 Mamba，专注优化 GEM-YOLO

论文参考：https://arxiv.org/abs/2511.19134
关键部分：Section II.B (HFAN) + Table II (消融实验)
```

---

### 任务 4（Phase 3：频域融合，长期探索）

> **WaveMamba 作为创新方向，建议在 GEM-YOLO 稳定后尝试**

```
目标：探索频域分解对 RGB-T 融合的价值
特点：
  - DWT 分解低频/高频，分别处理
  - 使用 Mamba 处理低频特征
  - 绝对最大值融合高频特征

前提条件：
  - 已完成 Phase 1 的 GEM-YOLO 实现
  - 已完成 Phase 2-B 的 Mamba 对比实验
  - 确认 Mamba 在本任务中有价值

论文参考：https://arxiv.org/abs/2507.18173
关键洞察：
  - IR 低频信息熵更高（目标轮廓/热辐射）
  - RGB 高频信息熵更高（纹理/边缘细节）
```

---

## 六、环境与硬件信息

```
操作系统：Windows（训练环境），macOS（代码编写）
GPU：NVIDIA GeForce RTX 5070 Ti (16GB VRAM)
CUDA：12.8
PyTorch：2.11.0+cu128
Ultralytics：8.4.67
Conda 环境：yolo_gaiic
数据集根目录：GAIIC2024-赛道1-目标检测任务/
```

---

## 七、数据集信息

```
训练集：17,990 对 RGB-TIR 图像，标注约 316,409 框
验证集：1,469 对，标注约 24,490 框
测试集：1,000 对（无标注，用于最终提交）
图像尺寸：640×512
类别：car(1), truck(2), bus(3), van(4), freight_car(5)
类别分布：car 占 84%（严重长尾），truck/bus/van/freight_car 各约 3~5%
```

**注意**：`freight_car` 类别最难检测（训练集仅 11,193 例），改进时需关注这类别的 AP。

---

## 八、关键代码位置

```
scripts/prepare_rgbt_yolo_dataset_v2.py   # 最新版数据集准备（支持多种融合方式）
scripts/fuse_multi_coco_results.py         # 多模型 WBF 集成（已有基础实现）
scripts/train_rgbt_yolo.py                 # 训练封装脚本
configs/gaiic_yolo_rgbt_aug_domain.yaml    # 当前最佳训练配置
work_dirs/yolo11s_rgbt_aug_domain/         # 当前最佳权重目录
submissions/                               # 提交文件目录
```

---

*文档生成时间：2026-06-21*
*基于论文调研结果整理，论文均已于 2025 年公开发表*
