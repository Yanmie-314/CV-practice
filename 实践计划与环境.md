# Ultralytics YOLO 实践计划与环境配置

## 1. 任务概述

本实践任务为无人机视角下的 RGB-T 双光目标检测。数据集包含成对的可见光图像和红外图像，目标是在训练集与验证集上训练车辆检测模型，并对测试集生成 COCO detection results 格式的 JSON 提交文件。

由于当前 Windows + RTX 5070 Ti + CUDA 12.8 环境下 MMCV 安装不稳定，本项目主线改为 Ultralytics YOLO。整体实验以 RGB-T 单模型为主线，同时保留 RGB-only、TIR-only 作为对照，并最终输出 COCO detection results 格式的 JSON 提交文件。

## 2. 数据集情况

数据目录：

```text
GAIIC2024-赛道1-目标检测任务/
├── train/
│   ├── rgb/
│   ├── tir/
│   └── train.json
├── val/
│   ├── rgb/
│   ├── tir/
│   └── val.json
└── test/
    ├── rgb/
    └── tir/
```

数据规模：

| 划分 | RGB 图像 | TIR 图像 | 图像对 | 标注框 |
| --- | ---: | ---: | ---: | ---: |
| train | 17,990 | 17,990 | 17,990 | 316,409 |
| val | 1,469 | 1,469 | 1,469 | 24,490 |
| test | 1,000 | 1,000 | 1,000 | 无 |

基本属性：

- 图像尺寸统一为 `640 x 512`。
- 标注为 COCO 检测格式。
- bbox 格式为 `[x, y, width, height]`。
- RGB 与 TIR 图像文件名一一对应。
- 每张训练图像平均约 17.59 个目标，属于密集检测场景。

类别：

| category_id | class |
| ---: | --- |
| 1 | car |
| 2 | truck |
| 3 | bus |
| 4 | van |
| 5 | freight_car |

类别分布：

| 划分 | car | truck | bus | van | freight_car |
| --- | ---: | ---: | ---: | ---: | ---: |
| train | 270,348 | 15,833 | 11,334 | 7,701 | 11,193 |
| val | 20,588 | 1,470 | 789 | 725 | 918 |

`car` 占比超过 84%，其余类别为长尾类别，训练和分析时需要重点关注类别不均衡。

## 3. Ultralytics YOLO 技术路线

### 3.1 第一阶段：RGB 单模态基线

先使用 COCO 预训练 YOLO 检测器训练 RGB-only 模型。

推荐基线：

- `yolo11s.pt`
- `yolo11m.pt`

优先建议从 `yolo11s.pt` 开始，原因是速度快、配置清晰、适合先跑通完整流程。

需要完成：

- 将 COCO 标注转换为 YOLO label 格式。
- 准备 `configs/gaiic_yolo_rgb.yaml`。
- 设置 5 个类别：`car`、`truck`、`bus`、`van`、`freight_car`。
- 指向 `train/rgb`、`val/rgb` 和测试集 `test/rgb`。
- 使用 YOLO COCO 预训练权重初始化。
- 在验证集上记录 bbox mAP 和各类别 AP。

### 3.2 第二阶段：TIR 单模态基线

复用 RGB 配置，改数据路径到：

- `train/tir`
- `val/tir`

保持相同模型、训练轮数、增强策略和评估方式，得到 TIR-only 结果。

该阶段用于判断红外模态在夜间、弱光、复杂背景下是否提供互补信息。

### 3.3 第三阶段：RGB-T 单模型输入级融合

RGB-T 单模型是本项目的主提交路线。训练前将同名 RGB/TIR 图像融合为一张空间对齐的 3 通道图，模型在单次前向中同时利用可见光和红外信息。

流程：

1. 读取成对的 RGB 图像和 TIR 图像。
2. 使用 `scripts/prepare_rgbt_yolo_dataset.py` 生成 `datasets/gaiic_yolo_rgbt`。
3. 使用 `configs/gaiic_yolo_rgbt.yaml` 训练单个 YOLO 模型。
4. 将预测结果转换为 COCO detection results 格式。
5. 在验证集上比较：
   - RGB-only
   - TIR-only
   - RGB-T 单模型

优点：

- 单个模型在一次推理中同时看到 RGB/TIR 信息。
- 不需要深改 Ultralytics 内部 Dataset 和增强流程。
- bbox 坐标保持原图坐标，推理结果可直接转换为 COCO detection results。

### 3.4 第四阶段：YOLO 双模态扩展

时间允许时再做结构级融合。

可选方向：

- 自定义 Dataset，同时读取 RGB 与 TIR。
- 自定义 transform，保证双模态同步 resize、flip、pad。
- 输入级融合：拼接为 6 通道图像，修改 backbone 第一层卷积。
- 双流 backbone：RGB/TIR 各一支 backbone，在 neck 或 head 前融合特征。

该阶段实现成本明显高于后融合，建议在已有稳定提交结果后再推进。

## 4. 训练策略

建议训练设置：

- 使用 COCO 预训练权重。
- 输入尺寸保持接近原始比例，建议 `640 x 512` 或常用的 `640 x 640` padded 输入。
- 开启 AMP 混合精度。
- 单卡训练时根据显存设置 batch size，RTX 5070 Ti 16GB 可先尝试 batch size 8，再按显存调整。
- 使用基础增强：resize、random flip、pad。
- 不建议一开始使用过强 Mosaic/MixUp，先建立稳定可复现基线。
- 长尾类别可后续尝试 class-balanced sampling、focal loss、调整各类别 score threshold。
- 密集目标场景可调优 NMS 阈值、score threshold、max_per_img。

## 5. 实验计划

| 实验 | 方法 | 目的 |
| --- | --- | --- |
| Exp1 | RGB-only YOLO11s | 建立可见光基线 |
| Exp2 | TIR-only YOLO11s | 建立红外基线 |
| Exp3 | RGB-T 单模型 YOLO11s | 验证单模型双光输入收益 |
| Exp4 | 更强模型，如 YOLO11m | 提升检测精度 |
| Exp5 | 类别不均衡优化 | 提升 truck/bus/van/freight_car |
| Exp6 | 自定义 RGB-T 融合模型 | 方法扩展与创新性 |

建议报告中至少保留 Exp1、Exp2、Exp3。

## 6. 项目结构

建议后续整理为：

```text
project/
├── configs/
│   ├── gaiic_yolo_rgb.yaml
│   ├── gaiic_yolo_tir.yaml
│   └── gaiic_yolo_fusion_notes.md
├── scripts/
│   ├── check_dataset.py
│   ├── visualize_coco.py
│   ├── prepare_yolo_dataset.py
│   ├── prepare_rgbt_yolo_dataset.py
│   ├── train_rgbt_yolo.py
│   ├── yolo_json_to_coco.py
│   ├── fuse_coco_results.py
│   ├── eval_coco_results.py
│   └── make_submission.py
├── datasets/
│   ├── gaiic_yolo_rgb/
│   ├── gaiic_yolo_tir/
│   └── gaiic_yolo_rgbt/
├── work_dirs/
│   ├── yolo11s_rgb/
│   ├── yolo11s_tir/
│   └── yolo11s_rgbt/
├── submissions/
├── requirements-yolo.txt
├── README.md
└── 实践计划与环境.md
```

## 7. YOLO 环境配置（RTX 50 系）

RTX 5070 Ti 属于新架构 GPU，低版本 PyTorch 与 CUDA 11.8 组合可能不支持该显卡架构。因此本项目环境配置不再优先追求旧版 MMDetection 兼容性，而应先保证 PyTorch 能正确使用 RTX 5070 Ti，再安装 Ultralytics 及其他依赖。

### 7.1 新建 Conda 环境

建议新建干净环境，统一命名为 `yolo_gaiic`：

```bash
conda create -n yolo_gaiic python=3.10 -y
conda activate yolo_gaiic
pip install -U pip
```

### 7.2 安装支持 RTX 5070 Ti 的 PyTorch

优先使用 CUDA 12.8 版本 PyTorch。目标检测不需要 `torchaudio`，不要把它放进基础安装命令，否则 pip 可能解析到旧版 `torchaudio` 并造成依赖冲突。

```bash
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
```

验证：

```bash
python -c "import torch; print('torch', torch.__version__); print('cuda', torch.version.cuda); print('available', torch.cuda.is_available()); print('gpu', torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'none'); print('capability', torch.cuda.get_device_capability(0) if torch.cuda.is_available() else 'none')"
```

期望结果包含：

```text
available True
gpu NVIDIA GeForce RTX 5070 Ti
```

### 7.3 安装项目依赖

```bash
pip install -r requirements-yolo.txt
```

其中 `requirements-yolo.txt` 已包含：

```text
ultralytics
opencv-python
pycocotools
matplotlib
tqdm
pillow
numpy
scipy
pandas
```

验证 Ultralytics：

```bash
python -c "import ultralytics; print('ultralytics', ultralytics.__version__)"
```

### 7.4 关于 MMDetection / MMCV 的说明

本项目主线不使用 MMDetection。如果后续想尝试 MMDetection，请参考《MMDetection环境配置.md》。需要注意的是，Windows + CUDA 12.8 + RTX 50 系下 MMCV 预编译 wheel 可能不可用，源码编译成功率不高，建议仅在 WSL2 / Linux 环境下尝试。

## 8. 后续运行约定

环境配置完成后，统一使用：

```bash
conda run --no-capture-output -n yolo_gaiic python ...
conda run --no-capture-output -n yolo_gaiic yolo ...
```

训练和推理这类长时间命令必须使用 `--no-capture-output`，否则 conda 可能缓存输出，终端看不到实时 epoch 日志。

### 8.1 环境检查

```bash
conda run --no-capture-output -n yolo_gaiic python -c "import torch; print(torch.__version__, torch.version.cuda, torch.cuda.is_available(), torch.cuda.get_device_name(0))"
conda run --no-capture-output -n yolo_gaiic python -c "import ultralytics; print('ultralytics', ultralytics.__version__)"
```

### 8.2 数据检查与准备

检查原始 COCO 数据集：

```bash
conda run --no-capture-output -n yolo_gaiic python scripts/check_dataset.py --data-root "GAIIC2024-赛道1-目标检测任务"
conda run --no-capture-output -n yolo_gaiic python scripts/visualize_coco.py --data-root "GAIIC2024-赛道1-目标检测任务" --split train --modality rgb
```

生成主线 RGB-T YOLO 格式数据集：

```bash
conda run --no-capture-output -n yolo_gaiic python scripts/prepare_rgbt_yolo_dataset.py --fusion rgbt --out-dir datasets/gaiic_yolo_rgbt --yaml-out configs/gaiic_yolo_rgbt.yaml --overwrite
```

输出：

```text
datasets/gaiic_yolo_rgbt/
configs/gaiic_yolo_rgbt.yaml
```

### 8.3 训练

RGB-T 单模型：

```bash
conda run --no-capture-output -n yolo_gaiic python scripts/train_rgbt_yolo.py --model yolo11s.pt --data configs/gaiic_yolo_rgbt.yaml --imgsz 640 --epochs 100 --batch 8 --device 0 --project D:/cv/work_dirs --name yolo11s_rgbt
```

RGB-only 和 TIR-only 可作为对照实验单独训练：

```bash
conda run --no-capture-output -n yolo_gaiic python scripts/prepare_yolo_dataset.py --modality rgb
conda run --no-capture-output -n yolo_gaiic python scripts/prepare_yolo_dataset.py --modality tir
conda run --no-capture-output -n yolo_gaiic yolo detect train model=yolo11s.pt data=configs/gaiic_yolo_rgb.yaml imgsz=640 epochs=100 batch=8 device=0 project=D:/cv/work_dirs name=yolo11s_rgb
conda run --no-capture-output -n yolo_gaiic yolo detect train model=yolo11s.pt data=configs/gaiic_yolo_tir.yaml imgsz=640 epochs=100 batch=8 device=0 project=D:/cv/work_dirs name=yolo11s_tir
```

### 8.4 验证与 COCO 结果生成

RGB-T 验证：

```bash
conda run --no-capture-output -n yolo_gaiic yolo detect predict model=D:/cv/work_dirs/yolo11s_rgbt/weights/best.pt source=datasets/gaiic_yolo_rgbt/images/val imgsz=640 conf=0.001 iou=0.7 max_det=300 save_json=True project=D:/cv/work_dirs name=yolo11s_rgbt_val_pred
conda run --no-capture-output -n yolo_gaiic python scripts/yolo_json_to_coco.py --split val --pred-json work_dirs/yolo11s_rgbt_val_pred/predictions.json --out submissions/rgbt_val.json
conda run --no-capture-output -n yolo_gaiic python scripts/eval_coco_results.py --pred submissions/rgbt_val.json
```

### 8.5 测试集推理与提交

RGB-T 测试：

```bash
conda run --no-capture-output -n yolo_gaiic yolo detect predict model=D:/cv/work_dirs/yolo11s_rgbt/weights/best.pt source=datasets/gaiic_yolo_rgbt/images/test imgsz=640 conf=0.001 iou=0.7 max_det=300 save_json=True project=D:/cv/work_dirs name=yolo11s_rgbt_test_pred
conda run --no-capture-output -n yolo_gaiic python scripts/yolo_json_to_coco.py --split test --pred-json work_dirs/yolo11s_rgbt_test_pred/predictions.json --out submissions/rgbt_test.json
conda run --no-capture-output -n yolo_gaiic python scripts/make_submission.py --input submissions/rgbt_test.json --out submissions/final_submission.json
```

最终提交 `submissions/final_submission.json`。

## 9. 交付物

最终需要形成：

1. YOLO RGB-T 数据集配置文件 `configs/gaiic_yolo_rgbt.yaml`。
2. 数据检查、可视化、转换、RGB-T 融合脚本。
3. RGB-T 单模型验证结果，并可附 RGB-only、TIR-only 对照结果。
4. 测试集 COCO JSON 提交文件。
5. README，包含环境配置、训练、验证、推理、提交生成方式。
6. 3 页以内实验报告。
7. 组内成员分工与贡献表。
