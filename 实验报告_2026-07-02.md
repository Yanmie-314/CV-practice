# 无人机双光目标检测实验报告

日期：2026-07-02  
项目路径：`D:\cv`  
任务：无人机视角下 RGB/TIR 双光目标检测，检测类别为 `car`、`truck`、`bus`、`van`、`freight_car`。

## 1. 实验目标

本实验围绕 GAIIC2024 赛道 1 目标检测任务展开，目标是在不破坏提交格式的前提下提升验证集与测试集的检测效果。实验重点包括：

1. 构建 RGB、TIR、RGB-T 融合输入的 YOLO 检测基线。
2. 通过域增强、不同 seed、大模型、GEM adapter 等方法提升模型表达能力。
3. 在推理阶段使用 TTA、WBF、多模型融合和按类别融合进一步提升最终提交效果。
4. 生成可提交的 COCO detection JSON 文件，并保证 bbox 精度和格式合法。

## 2. 数据与评估方式

原始数据包含 `train`、`val`、`test` 三个划分，每张图像尺寸为 `640 x 512`。验证集标注统计如下：

| 类别 | category_id | val 标注数 |
| --- | ---: | ---: |
| car | 1 | 20588 |
| truck | 2 | 1470 |
| bus | 3 | 789 |
| van | 4 | 725 |
| freight_car | 5 | 918 |

训练过程中 Ultralytics 输出的 `metrics/mAP50-95(B)` 与提交前使用的 COCO JSON 评估口径不同：

- 训练日志 mAP：Ultralytics 内部 validator 直接基于 YOLO 数据配置计算。
- 提交口径 AP：先导出 COCO detection JSON，再使用 `pycocotools.COCOeval` 评估。
- 本报告的提交选择主要依据 COCO JSON 评估结果，因为它更接近最终评测端。

## 3. 环境与主要工具

| 项目 | 配置 |
| --- | --- |
| Conda 环境 | `yolo_gaiic` |
| Python | 3.10 |
| PyTorch | 2.11.0 + CUDA 12.8 |
| Ultralytics | 8.4.67 |
| GPU | NVIDIA GeForce RTX 5070 Ti |
| 主要评估脚本 | `scripts/eval_coco_results.py`、`scripts/evaluate_coco_batch.py` |
| 主要融合脚本 | `scripts/fuse_multi_coco_results.py`、`scripts/search_coco_fusion.py`、`scripts/combine_coco_by_category.py` |

## 4. 方法思路与策略介绍

### 4.1 RGB-T 输入级融合

本任务同时提供可见光 RGB 图像和热红外 TIR 图像。RGB 图像包含纹理、颜色和边缘信息，TIR 图像对光照变化更稳健，能够在弱光、阴影或背景复杂场景中提供补充信息。因此，本实验没有只依赖单一模态，而是将 TIR 与 RGB 的部分通道组合成 3 通道 RGB-T 融合图像。

该策略的优点是工程实现简单，可以直接复用 YOLO 的 COCO 预训练权重，不需要大幅修改检测网络结构。缺点是融合发生在输入层，模型不能显式学习两种模态在不同场景下的贡献权重，因此后续又通过 GEM adapter 和 WBF 来弥补输入级融合的不足。

### 4.2 测试域增强

训练集和测试集在亮度、对比度、模糊程度、压缩噪声和 TIR 分布上存在一定差异。为减小这种域偏移，本实验根据训练集和测试集的统计差异构造了测试域增强数据集。

增强策略主要包括：

- 调整 RGB 亮度、对比度和 gamma。
- 调整 TIR 亮度和对比度。
- 加入轻微模糊、噪声和 JPEG 压缩。
- 所有增强均为像素级变换，不改变 bbox 标注。

该策略的目标不是制造过强扰动，而是让训练数据更接近测试集的成像分布，从而提升泛化能力。实验中，YOLO11s RGB-T 在测试域增强数据上训练后，比原始 RGB-T 训练更稳定。

### 4.3 大模型升级

在 YOLO11s 基线稳定后，进一步使用 YOLO11m 进行训练。大模型拥有更强的特征提取能力和更大的参数容量，对于无人机视角下密集、小目标、多尺度车辆检测更有优势。

大模型策略的核心思路是：先用 YOLO11s 快速验证数据处理和融合方向，再将已经验证有效的数据配置迁移到 YOLO11m 上。这样可以降低试错成本，避免一开始就在大模型上进行大量低效实验。

### 4.4 GEM Adapter

GEM adapter 是在已有 YOLO11s RGB-T 检测器前加入的轻量输入适配模块。它不重新训练完整检测器，而是在输入端学习一个小的动态融合调整，使输入图像更适合已经训练好的 YOLO 检测器。

该方法的策略价值主要体现在两点：

1. 额外参数量很小，训练和推理成本较低。
2. 它与普通 YOLO11m RGB-T 模型的错误分布不同，适合作为 WBF 融合中的辅助成员。

虽然 GEM adapter 单独验证 AP 不如 YOLO11m TTA，但它在融合中提供了互补信息，尤其对部分类别和小目标有帮助。

### 4.5 TTA 测试时增强

TTA 的思路是在推理阶段对同一张图像使用翻转、尺度变化等增强方式进行多次预测，再合并预测结果。它不需要重新训练模型，只增加推理时间，因此是低风险的推理侧提分方法。

本实验中，YOLO11m 单模型开启 TTA 后，验证集 COCO JSON AP 从 `0.6098` 提升到 `0.6229`。提升主要来自召回率和 small AP 的提高，说明 TTA 对密集小目标检测有实际帮助。

### 4.6 WBF 加权框融合

WBF 的核心思想是：多个模型对同一目标给出的检测框位置可能略有差异，如果直接 NMS 只保留最高分框，可能会丢失其他模型的定位信息；而 WBF 会根据置信度和模型权重对同一目标的多个框做加权平均，从而获得更稳定的框位置和分数。

本实验使用 WBF 融合 `YOLO11m RGBT TTA` 和 `GEM adapter last`。搜索发现，adapter 的最优权重并不是越低越好。在全局融合中，`weights=1.0/1.1` 比旧方案 `1.0/0.7` 更优，说明 adapter 对最终结果有较强互补作用。

### 4.7 按类别专属融合

不同类别的目标大小、外观和类别混淆情况不同，统一 WBF 参数难以同时兼顾所有类别。例如，`bus` 目标通常更大且形状稳定，而 `van`、`freight_car` 更容易与其他车辆混淆。因此，本实验进一步按类别分别搜索 WBF 的 `iou-thr`、模型权重和 score 阈值。

最终策略是对每个类别单独生成最优 WBF 结果，再按 `category_id` 拼接成最终提交文件。该方法相比统一参数 WBF 有进一步提升，是最终最佳方案。

## 5. 训练结果

主要训练结果如下。该表为 Ultralytics 训练日志口径，用于反映训练过程中的模型能力。

| 训练目录 | best epoch | mAP50:95 | mAP50 | Precision | Recall |
| --- | ---: | ---: | ---: | ---: | ---: |
| `yolo11m_rgbt_aug_domain_w0` | 96 | 0.6622 | 0.8719 | 0.8435 | 0.8260 |
| `yolo11s_rgbt_aug_domain-2` | 119 | 0.6511 | 0.8640 | 0.8260 | 0.8241 |
| `yolo11s_rgbt_aug_domain` | 88 | 0.6507 | 0.8627 | 0.8418 | 0.8110 |
| `yolo11s_rgbt_aug_domain_seed2` | 82 | 0.6506 | 0.8647 | 0.8464 | 0.8062 |
| `yolo11s_rgbt_aug_domain_seed1` | 72 | 0.6458 | 0.8584 | 0.8394 | 0.8024 |
| `yolo11s_rgbt` | 87 | 0.6435 | 0.8538 | 0.8461 | 0.7924 |
| `yolo11s_tir` | 59 | 0.6335 | 0.8483 | 0.8232 | 0.8021 |

结论：

- 域增强 RGB-T 数据集相比原始 RGB-T 数据集更稳定，YOLO11s 的 mAP50:95 从约 `0.6435` 提升到约 `0.6507`。
- YOLO11m 大模型进一步提升到训练日志口径 `0.6622`，是后续推理融合的主力模型。
- RGB-only 模型显著弱于 RGB-T/TIR 相关模型，单独作为最终融合成员收益有限。

## 6. 推理与融合实验

### 6.1 TTA 实验

| 方案 | AP50:95 | AP50 | small AP |
| --- | ---: | ---: | ---: |
| YOLO11m 单模型 | 0.6098 | 0.8035 | 0.2484 |
| YOLO11m + TTA | 0.6229 | 0.8175 | 0.2609 |

TTA 的主要收益来自召回率和小目标检测能力提升。

### 6.2 GEM Adapter 实验

| 方案 | AP50:95 | AP50 | small AP |
| --- | ---: | ---: | ---: |
| GEM adapter last | 0.6160 | 0.8115 | 0.2600 |

GEM adapter 单独 AP 不如 YOLO11m TTA，但与 YOLO11m 的错误分布不同，适合作为 WBF 辅助成员。

### 6.3 全局 WBF 搜索

| 方案 | WBF 参数 | AP50:95 | AP50 | small AP |
| --- | --- | ---: | ---: | ---: |
| 旧复现大模型融合 | `iou=0.75, weights=1.0/0.7, score=0.001` | 0.6298 | 0.8210 | 0.2649 |
| 优化后全局融合 | `iou=0.74, weights=1.0/1.1, score=0.0005` | 0.6315 | 0.8263 | 0.2732 |

### 6.4 按类别专属 WBF

| 方案 | AP50:95 | AP50 | AP75 | small AP | medium AP | large AP |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| YOLO11m TTA 单模型 | 0.6229 | 0.8175 | 0.7462 | 0.2609 | 0.6223 | 0.8099 |
| 全局 WBF 优化 | 0.6315 | 0.8263 | 0.7565 | 0.2732 | 0.6311 | 0.8111 |
| YOLO11s classwise WBF | 0.6258 | 0.8225 | 0.7479 | 0.2670 | 0.6239 | 0.8189 |
| YOLO11m classwise WBF | **0.6337** | **0.8269** | **0.7574** | **0.2756** | **0.6324** | 0.8155 |

类别级 AP：

| 方案 | car | truck | bus | van | freight_car |
| --- | ---: | ---: | ---: | ---: | ---: |
| YOLO11m classwise WBF | 0.7133 | 0.6025 | 0.7923 | 0.4869 | 0.5734 |
| YOLO11s classwise WBF | 0.7096 | 0.5743 | 0.7873 | 0.4864 | 0.5714 |

最终 YOLO11m classwise WBF 参数：

| 类别 | 参数 |
| --- | --- |
| car | `iou=0.72, weights=1.0/1.25, score=0.0005` |
| truck | `iou=0.75, weights=1.0/0.95, score=0.0005` |
| bus | `iou=0.74, weights=1.0/0.65, score=0.0005` |
| van | `iou=0.76, weights=1.0/1.1, score=0.0005` |
| freight_car | `iou=0.72, weights=1.0/1.1, score=0.0005` |

### 6.5 YOLO11s Classwise 融合尝试

本实验还尝试将 YOLO11s classwise WBF 作为二级辅助成员继续融合到 YOLO11m classwise WBF 中，但验证集 AP 降低到约 `0.6236`。这说明弱成员并不一定能提升融合效果，如果其误检或定位偏差较多，反而会干扰强模型结果。因此最终没有采用该方案。

## 7. 最终提交文件

首选提交文件：

```text
submissions/final_yolo11m_classwise_wbf_best_bbox3.json
submissions/final_yolo11m_classwise_wbf_best_bbox3.zip
```

提交文件统计：

| 文件 | 检测框数 | 大小 | 状态 |
| --- | ---: | ---: | --- |
| `final_yolo11m_classwise_wbf_best_bbox3.json` | 83856 | 6.95 MB | 已通过格式校验 |
| `final_yolo11m_classwise_wbf_best_bbox3.zip` | - | 1.59 MB | 已生成 |

完整性检查：

- `category_id` 覆盖 `[1, 2, 3, 4, 5]`。
- bbox 宽高均大于 0。
- bbox 小数保留 3 位，避免极小框被四舍五入为 0。
- score 小数保留 6 位。
- 已通过 `scripts/make_submission.py` 校验。

备选提交文件：

| 优先级 | 文件 | 说明 |
| ---: | --- | --- |
| 1 | `submissions/final_yolo11m_classwise_wbf_best_bbox3.json` | 当前验证集最优，推荐提交 |
| 2 | `submissions/final_yolo11m_tta_adapter_wbf074_w110_score0005_bbox3.json` | 全局 WBF 优化版本 |
| 3 | `submissions/final_yolo11s_classwise_wbf_best_bbox3.json` | YOLO11s-only 备选 |
| 4 | `submissions/final_reproduce_big_yolo11m_tta_adapter_wbf075_bbox3.json` | 旧复现基线 |

## 8. 主要结论

1. RGB-T 融合输入显著优于 RGB-only，TIR 信息对该任务非常关键。
2. 测试域增强能稳定提升 YOLO11s RGB-T 训练效果。
3. YOLO11m 大模型是当前最强单模型路线，配合 TTA 后 COCO JSON AP 明显提升。
4. GEM adapter 虽然单独 AP 不最高，但作为 WBF 辅助模型能够提供互补信息。
5. 按类别搜索 WBF 参数比统一参数更优，最终将 COCO JSON AP50:95 提升到 `0.6337`。
6. 弱模型并不一定适合继续加入融合，YOLO11s classwise 结果融合进 YOLO11m classwise 后反而降低 AP。

## 9. 心得体会

本次实验最大的体会是，目标检测竞赛中的最终效果不仅取决于训练阶段的 mAP，还高度依赖推理、后处理和提交格式。训练日志中的指标可以帮助判断模型是否收敛，但最终提交前必须使用与评测端更接近的 COCO JSON 链路进行验证，否则容易出现训练指标较高但提交效果不理想的情况。

第二个体会是，多模态任务不能简单地认为“模型越多融合越好”。RGB、TIR、RGB-T、GEM adapter 和不同 seed 模型之间确实可能互补，但如果辅助模型本身误检较多，或者定位偏差与主模型不一致，融合后反而会拉低 AP。因此，融合成员需要通过验证集严格筛选，不能只根据单模型分数或主观经验决定。

第三个体会是，按类别分析非常重要。本任务中 `car` 数量最多，容易主导整体指标，而 `truck`、`van`、`freight_car` 等类别数量少、混淆更明显。统一后处理参数无法同时照顾所有类别，按类别搜索 WBF 参数后，整体 AP 得到了进一步提升。这说明在类别分布不均衡的数据集中，类别级诊断和类别级策略往往比单一全局策略更有效。

最后，本实验也体现了工程流程的重要性。每次生成提交文件都需要检查 bbox 宽高、类别编号、小数精度和 JSON 格式，尤其是极小框在四舍五入后可能变成 0。稳定的评估脚本、融合脚本和提交校验脚本能够减少低级错误，让主要精力集中在模型与策略优化上。

